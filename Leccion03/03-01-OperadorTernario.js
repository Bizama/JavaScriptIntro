// Condition ? value_if_True : value_if_False
let resultado = ( 4 > 2 ) ? true : 10;
console.log(resultado);

// Rescribir ejercicio para saber si es par o impar

let numero = 101;
resultado = ( numero % 2 == 0 ) ? 'Es un numero par' : 'Es un numero impar';
console.log(resultado);

