// Modo Strict en JavaScript.
"use strict"
// Es para no poder utilizar variables que no han sido definidas. 
// Se puede usar dentro de una funcion.
let x = 10;
let y = 10; 
console.log(x);
console.log(y);